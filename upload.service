[Unit]
Description=Go Upload Resource Server
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/upload
ExecStart=/usr/local/bin/upload -config /opt/upload/.env
Restart=always
RestartSec=2
User=www-data
Group=www-data

[Install]
WantedBy=multi-user.target
